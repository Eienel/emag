{
  "name": "@shadowpay/contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts --network arbitrumSepolia",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost",
    "node": "hardhat node",
    "verify": "hardhat run scripts/verify.ts --network arbitrumSepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.10",
    "@types/node": "^20.11.30",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "@iexec-nox/nox-confidential-contracts": "^0.1.0",
    "@iexec-nox/nox-protocol-contracts": "^0.1.0",
    "@openzeppelin/contracts": "^5.1.0"
  }
}
